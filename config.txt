# FortiAI Pro Configuration
# Edit this file before loading extension to pre-configure settings
# Lines starting with # are comments

# Groq API Key (Moodle mode)
# Get your key at: https://console.groq.com/keys
GROQ_KEY=

# Model policy
PRIMARY_MODEL=openai/gpt-oss-120b
FALLBACK_MODEL=llama-3.1-8b-instant
MODEL_PROVIDER_ORDER=groq,ollama
ENABLE_FALLBACK=true
ENABLE_ARBITRATION=false

# Ollama settings (used when provider order includes ollama)
OLLAMA_URL=http://localhost:11434/api/chat
OLLAMA_MODEL=llama3.1:8b

# Proxy URL (Cloudflare Worker)
# Leave empty to use direct API calls (not recommended)
PROXY_URL=https://safeexambrowser.signalrwebsocket.workers.dev/

# Debug mode: true or false
DEBUG=false
